import React from "react"
import { Link } from "gatsby"

const Navbar = () => {
  return (
    <nav>
      <ul>
        <Link>Home</Link>
        <Link>About</Link>
      </ul>
    </nav>
  )
}

export default Navbar
